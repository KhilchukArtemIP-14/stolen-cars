TypeORM (but it uses Mongoose)

MongoDB Atlas with replica sets (but localhost only)

CSRF protection via csurf middleware (not installed)

Helmet for security headers (not present)
Also add a CONTRIBUTING.md that says “All new features must use the GraphQL API first” – but GraphQL is completely broken.